# Startup measurement

`docs/roadmap.md` reserves a slot for startup work and attaches a condition to
it: **the numbers come first, so that anything done afterwards is comparable.**
This file holds the numbers that exist so far, and — just as deliberately —
says which ones do not.

It is separate from [`performance-baseline.md`](performance-baseline.md)
because that file is generated by `dbboard-bench` and overwritten by the next
run. Launching an application and waiting for its window is not something that
harness can do: the thing being timed is a process and a window server, not a
function.

Nothing here is asserted by a test. A threshold on a timing is a flaky test,
and this project has already paid for one (ADR-0125).

## What was measured

| | |
|---|---|
| Hardware | Mac mini (Apple M1, 2020), 16 GB |
| OS | macOS 15.x |
| Build under test | **dbboard 0.16.0**, the release installed at the time |
| Date | 2026-09-11 |
| Method | `open -a dbboard.app`, then poll System Events until the process has a window; 9 samples, each after quitting the app and waiting 2 s |

## Relaunch to a window on screen

| Samples | Median | Min | Max |
|---|---|---|---|
| 9 | **318 ms** | 270 ms | 916 ms |

The individual runs: 270, 292, 298, 302, 318, 319, 346, 637, 916 ms.

**Seven of nine land between 270 and 346 ms.** The two that do not are 2× and
3× the median, and nine samples cannot say whether that is the window server,
this machine's other load, or something in the app. It is recorded rather than
averaged away: a median of 318 ms with a tail at 916 ms is a different
experience from a steady 318 ms, and the tail is the part a person notices.

## What this number is not

- **Not a cold start.** Each sample follows a quit, so the binary and its
  libraries are still in the page cache. A genuine cold start — the first
  launch after a boot — is one sample per reboot and was not taken.
- **Not "usable".** What is timed is the window *existing*, which is when the
  window server hands one over. The webview paints its content after that, and
  this method cannot see the difference. Treat the number as a floor.
- **Not connected to anything.** Time from picking a connection to a populated
  schema tree, and from Run to the first row, need a real database, and the
  roadmap asks for a real one rather than a synthetic. Neither is measured.
- **Not the current release.** 0.16.0 was what was installed. Two releases have
  shipped since; nothing in them targeted startup, but the number describes the
  build it describes.

## What would close the gap

1. ~~**First paint**, separated from window creation.~~ **The app can say this
   now** (ADR-0156). The shell stamps a clock as the first statement of
   `run()`, the frontend reports after the frame that actually presented
   something, and `startup_timing` answers both `first_paint_ms` and
   `uptime_ms`. Nothing here takes samples — that is still a person launching
   the app and reading the number back — but the number is no longer
   unobtainable.

   **Where to read it: the About dialog.** Launch the app, open *dbboard
   について* / *About dbboard*, and the figure sits under the version as
   **First paint**. A dash means the report has not landed — which outside a
   Tauri runtime it never will.

   It is not on the MCP surface. `dbboard-mcp` is a separate process and
   cannot see the window's clock; reaching it would mean a new UI command
   (ADR-0109), which is more machinery than reading a number off a dialog
   deserves. If sampling ever needs to be automated, that is the door.

   The shell also keeps `uptime_ms` alongside it, because a paint at 900 ms
   reads differently when the process has been up 900 ms than when it has been
   up 40 seconds — the second says the report arrived late, not that painting
   was slow.
2. **Connect and browse**, and **Run to first row**, against a real connection,
   with the operator present to choose it.
3. **A cold start**, taken once after a reboot.

Until then the roadmap item stays unticked. A quarter of a measurement is not
a measurement, and the point of taking it first was to have something to
compare against later.

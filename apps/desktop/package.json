{
  "name": "@dbboard/desktop",
  "version": "0.3.0",
  "private": true,
  "description": "dbboard desktop spike (Tauri 2 + SvelteKit) — proves the WebView↔core path against the egui-free Rust core.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.9.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.2",
    "@tauri-apps/cli": "^2.1.0",
    "svelte": "^5.12.0",
    "svelte-check": "^4.1.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  },
  "packageManager": "pnpm@11.1.1",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"],
    "minimumReleaseAge": 1440
  }
}

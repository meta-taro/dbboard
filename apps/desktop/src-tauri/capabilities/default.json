{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike capability set: the main window may invoke the three read-only dbboard commands. Core (non-app-specific) permissions only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
